# Development profile
spring.datasource.url=${DEV_DB_URL:jdbc:mysql://localhost:3306/acare_clinic_3}
spring.datasource.username=${DEV_DB_USERNAME:root}
spring.datasource.password=${DEV_DB_PASSWORD:Hung12062005@}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Dev: Flyway quản lý schema, Hibernate chỉ validate — không tự tạo/sửa bảng
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# CORS — cho phép cả web app local và mobile emulator
app.security.cors.allowed-origins=${DEV_CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:3000,http://10.0.2.2:8080}

app.security.cookie.same-site=Lax
app.security.cookie.secure=false
app.security.cookie.domain=
app.security.cookie.path=/

# Admin bootstrap (supports both dev/prod)
app.bootstrap.admin.enabled=${DEV_BOOTSTRAP_ADMIN_ENABLED:true}
app.bootstrap.admin.full-name=${DEV_BOOTSTRAP_ADMIN_FULL_NAME:System Administrator}
app.bootstrap.admin.email=${DEV_BOOTSTRAP_ADMIN_EMAIL:admin@acare.local}
app.bootstrap.admin.phone=${DEV_BOOTSTRAP_ADMIN_PHONE:0975769006}
app.bootstrap.admin.id-number=${DEV_BOOTSTRAP_ADMIN_ID_NUMBER:0605009361}
app.bootstrap.admin.address=${DEV_BOOTSTRAP_ADMIN_ADDRESS:284 Te Lo, Phu Tho}
app.bootstrap.admin.password=${DEV_BOOTSTRAP_ADMIN_PASSWORD:ChangeMe@123}

logging.level.root=INFO
logging.level.com.acare.backend=DEBUG
